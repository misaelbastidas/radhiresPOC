{
  "name": "invoice-exception-desk",
  "version": "0.1.0",
  "private": true,
  "description": "A portable invoice duplicate review harness with deterministic OCR and optional vision-model extraction.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --configLoader native",
    "dev:api": "node server.mjs",
    "build": "vite build --configLoader native",
    "start": "node server.mjs",
    "test": "node --test",
    "eval": "node scripts/evaluate_corpus.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@langchain/anthropic": "^1.5.11",
    "@langchain/core": "^1.2.12",
    "@langchain/langgraph": "^1.4.17",
    "langchain": "^1.5.12",
    "pdfjs-dist": "^4.10.38",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tesseract.js": "^6.0.1",
    "vite": "^6.0.5",
    "zod": "^4.6.5"
  }
}
